claude-agent-sdk>=0.0.47
# Security floors (bounded, not pinned - Dependabot can still raise them).
# pytest  >=9.0.3 clears GHSA for pytest <9.0.3.
# pygments>=2.20.0 clears GHSA for Pygments <2.20.0 (arrives via pytest, declared
# explicitly so the floor is enforced rather than incidental).
pytest>=9.0.3,<10
pygments>=2.20.0,<3
pytest-asyncio>=0.24.0
